*** Settings ***
Library     Collections
Library     mongo.MongoDB       localhost:27017     billing
Library     restRequest.RestRequest
#Suite Setup     suiteInitialization


*** Variable ***
${REST_SERVER_ADDRESS}		http://localhost:9002


*** Keywords ***
suiteInitialization
    deleteDatabase   billing




*** Test Cases ***

Scrape the website by identifier
    # find the billable entity by id
    ${identifier} =     Set Variable        freecodecamp-python-latest
    ${response} =       postJson 	${REST_SERVER_ADDRESS}/api/v1.0/scrape/identifier/${identifier}          data={}
    ${statusCode} =     Set Variable    ${response.status_code}
    Should Be Equal As Integers        ${statusCode}       200


